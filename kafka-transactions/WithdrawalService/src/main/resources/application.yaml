server:
  port: 0

spring:
  application:
    name: WithdrawalService

  kafka:
    consumer:
      bootstrap-servers: localhost:9092
      group-id: amount-widthrawal-event
      properties:
        spring.json.trusted.packages: com.appsdeveloperblog.*
      isolation-level: read_committed  # read only successfully commited messages